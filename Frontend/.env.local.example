# Frontend env. Copy to .env.local
#   copy .env.local.example .env.local

# FastAPI backend (backend/app/main.py). Must match CORS_ORIGIN's counterpart there,
# and the session cookie only rides along because the API client sends credentials.
NEXT_PUBLIC_API_BASE=http://localhost:8000
